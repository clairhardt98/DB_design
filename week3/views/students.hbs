<h1>{{ title }}</h1>

<ul>
  {{#each students}}
    <li>
        {{ StudentNumber }} / {{ Name }} / {{ Class }} / {{ Major }} / {{ Date }} / {{ Email }}
      </a>
    </li>
  {{/each}}
</ul>